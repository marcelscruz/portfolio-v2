export const generalConfiguration = `
    a {
        color: var(--secondary);
        text-decoration: none;
    }
    
    a:visited {
        color: var(--secondary);
    }
`

export default generalConfiguration
